automation "Luz quando abrir porta" (DoorOpenEvent, Abc) {
    run echo("Ligar luz!")
    run echo("si")
    run echo("LAS!")
}

automation DummyAutomation (Dummy) {
  run echo("dummy hello world!")
}
