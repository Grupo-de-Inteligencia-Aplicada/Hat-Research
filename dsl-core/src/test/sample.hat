automation ConstExpr (Dummy) {
    if false
    run echo("should not be printed")
}

automation "Luz quando abrir porta" (DoorOpenEvent, Abc) {
    run echo("Ligar luz!")
    run echo("si")
    run echo("LAS!")
}

automation DummyAutomation (Dummy) {
  if is_device("dummy-device-2707")
  run echo("dummy hello world!")
}

